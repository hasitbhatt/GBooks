# Simple and without proxy #
gets the width, url, prefix and postfix and surfs the pages according to innnputs and fetches the link of image and tries to download it in folder with same name as book id.
